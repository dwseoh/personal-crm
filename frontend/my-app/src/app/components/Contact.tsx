"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";

interface Contacts {
    name: string;
}

export default function Contact({name}: Contacts) {
    return(
        <main>

        </main>
    )

}